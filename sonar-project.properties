# =============================================================================
# SonarCloud — configuration du projet
# =============================================================================

sonar.projectKey=evanferron_rust-api-template
sonar.organization=evanferron

sonar.projectName=rust-api-template
sonar.projectVersion=1.0

# Sources analysées
sonar.sources=src
sonar.exclusions=src/bin/**,**/schema.rs

# Rapport de coverage généré par cargo-llvm-cov
sonar.coverageReportPaths=lcov.info

# Langue
sonar.language=rust